import styled from 'styled-components';

export const Container = styled.div`
  width: 100vw;
  min-height: 100vh;
  padding: 0 4rem;

  display: flex;
  flex-direction: column;

  > main {
    flex: 1;
  }
`;
